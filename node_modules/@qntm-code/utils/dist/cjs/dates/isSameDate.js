"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isSameDate = void 0;
const convert_units_1 = require("./convert-units");
const getters_1 = require("./getters");
/**
 * Determines if two dates are the same. If you want to limit the granularity to a unit other than milliseconds, pass it as the second
 * parameter.
 *
 * When including a second parameter, it will match all units equal or larger. For example, if passing in month will check month and year,
 * or if passing in day will check day, month, and year.
 */
function isSameDate(a, b, unit) {
    if (a.getFullYear() !== b.getFullYear()) {
        return false;
    }
    if (unit === convert_units_1.TimeUnit.Year || unit === convert_units_1.TimeUnit.Years) {
        return true;
    }
    if (a.getMonth() !== b.getMonth()) {
        return false;
    }
    if (unit === convert_units_1.TimeUnit.Month || unit === convert_units_1.TimeUnit.Months) {
        return true;
    }
    if ((0, getters_1.getStartOfWeek)(a).getDate() !== (0, getters_1.getStartOfWeek)(b).getDate()) {
        return false;
    }
    if (unit === convert_units_1.TimeUnit.Week || unit === convert_units_1.TimeUnit.Weeks) {
        return true;
    }
    if (a.getDate() !== b.getDate()) {
        return false;
    }
    if (unit === convert_units_1.TimeUnit.Day || unit === convert_units_1.TimeUnit.Days) {
        return true;
    }
    if (a.getHours() !== b.getHours()) {
        return false;
    }
    if (unit === convert_units_1.TimeUnit.Hour || unit === convert_units_1.TimeUnit.Hours) {
        return true;
    }
    if (a.getMinutes() !== b.getMinutes()) {
        return false;
    }
    if (unit === convert_units_1.TimeUnit.Minute || unit === convert_units_1.TimeUnit.Minutes) {
        return true;
    }
    if (a.getSeconds() !== b.getSeconds()) {
        return false;
    }
    if (unit === convert_units_1.TimeUnit.Second || unit === convert_units_1.TimeUnit.Seconds) {
        return true;
    }
    return a.getMilliseconds() === b.getMilliseconds();
}
exports.isSameDate = isSameDate;
