/**
 * Return whether an element is `display: inline`
 */
export function isDisplayInline(element) {
    if (element.nodeType === 3) {
        return true;
    }
    const computedStyle = window.getComputedStyle(element);
    return computedStyle.display === 'inline';
}
