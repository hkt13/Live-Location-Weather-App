import { setEndOfDay } from '../setters';
/**
 * Takes an optional date and returns a new Date object set to the end of the given/current day
 */
export function getEndOfDay(date = new Date()) {
    return setEndOfDay(new Date(date));
}
