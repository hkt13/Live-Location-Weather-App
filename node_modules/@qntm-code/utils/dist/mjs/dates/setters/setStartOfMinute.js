import { setStartOfSecond } from '.';
/**
 * Takes a given date and mutates it to the start of the given minute
 */
export function setStartOfMinute(date) {
    setStartOfSecond(date);
    date.setSeconds(0);
    return date;
}
